[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
requires-python = ">=3.10"
name = "prueba"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "ipython>=8.10",
    "jupyterlab>=3.0",
    "notebook",
    "kedro~=1.4.0",
    "kedro-datasets[pandas-csvdataset,pandas-parquetdataset,pandas-sqlquerydataset]>=3.0",
    "pandas>=2.3.3",
    "pyarrow>=15.0",
    "requests>=2.31",
    "SQLAlchemy>=2.0",
    "psycopg2-binary>=2.9",
    "kedro-viz>=12.4.0",
    "scikit-learn>=1.3",
]

[project.scripts]
"prueba" = "prueba.__main__:main"

[project.entry-points."kedro.hooks"]

[project.optional-dependencies]
docs = [
    "docutils<0.21",
    "sphinx>=5.3,<7.3",
    "sphinx_rtd_theme==2.0.0",
    "nbsphinx==0.8.1",
    "sphinx-autodoc-typehints==1.20.2",
    "sphinx_copybutton==0.5.2",
    "ipykernel>=5.3, <7.0",
    "Jinja2<3.2.0",
    "myst-parser>=1.0,<2.1"
]
dev = [
    "pytest-cov>=3,<7",
    "pytest-mock>=1.7.1, <2.0",
    "pytest~=7.2",
    "ruff~=0.12.0"
]

[tool.setuptools.dynamic]
version = {attr = "prueba.__version__"}

[tool.setuptools.packages.find]
where = ["src"]
namespaces = false

[tool.kedro]
package_name = "prueba"
project_name = "Prueba"
kedro_init_version = "1.4.0"
tools = "['Linting', 'Testing', 'Custom Logging', 'Documentation', 'Data Structure']"
example_pipeline = "False"
source_dir = "src"

[tool.pytest.ini_options]
addopts = """
--cov-report term-missing \
--cov src/prueba -ra"""

[tool.coverage.report]
fail_under = 0
show_missing = true
exclude_lines = ["pragma: no cover", "raise NotImplementedError"]

[tool.ruff.format]
docstring-code-format = true

[tool.ruff]
line-length = 88
show-fixes = true

[tool.ruff.lint]
select = [
    "F",   # Pyflakes
    "W",   # pycodestyle
    "E",   # pycodestyle
    "I",   # isort
    "UP",  # pyupgrade
    "PL",  # Pylint
    "T201", # Print Statement
]
ignore = [
    "E501",    # Ruff format takes care of line-too-long
    "T201",    # los scripts CLI usan print intencionalmente
]

[tool.ruff.lint.per-file-ignores]
# Los tests usan valores literales en asserts e imports diferidos legítimos.
"tests/*" = ["PLR2004", "PLC0415"]
# Los nodos Kedro reciben múltiples datasets como argumentos por diseño.
"src/prueba/pipelines/processing/nodes.py" = ["PLR0913"]
"src/prueba/utils/io_sources.py" = ["PLR0913"]

[tool.kedro_telemetry]
project_id = "365f522d3c0e4f97b5a4081286afddc3"
